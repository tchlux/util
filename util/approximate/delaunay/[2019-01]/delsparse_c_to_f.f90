!     Module generated by fmodpy for fortran file named 'DELAUNAY'     
!=====================================================================
MODULE DELAUNAY_WRAPPER
  ! Any necessary "USE" statements
  USE REAL_PRECISION
  USE DELSPARSE_MOD
  ! Make sure it is clear that no implicit types are used in this wrapper
  IMPLICIT NONE
  ! Any interfaces defined for "PROCEDURE" declarations
  
CONTAINS
  !     Fortran wrapper for DELAUNAYSPARSES, callable from C     
  !========================================================

   SUBROUTINE c_DELAUNAYSPARSES( D, N, PTS_0, PTS_1, PTS, M, Q_0, Q_1,&
     & Q, SIMPS_0, SIMPS_1, SIMPS, WEIGHTS_0, WEIGHTS_1, WEIGHTS, IERR&
     &_0, IERR, INTERP_IN_0, INTERP_IN_1, INTERP_IN, INTERP_IN_PRESENT&
     &, INTERP_OUT_0, INTERP_OUT_1, INTERP_OUT, INTERP_OUT_PRESENT, EP&
     &S, EPS_PRESENT, EXTRAP, EXTRAP_PRESENT, RNORM_0, RNORM, RNORM_PR&
     &ESENT, IBUDGET, IBUDGET_PRESENT, CHAIN, CHAIN_PRESENT ) BIND(c)
    ! This subroutine's only purpose is to call the source fortran
    !  code while also being accessible from C.
    INTEGER, INTENT(IN) :: D
    INTEGER, INTENT(IN) :: N
    INTEGER, INTENT(IN) :: PTS_0
    INTEGER, INTENT(IN) :: PTS_1
    REAL(KIND=R8), DIMENSION(PTS_0,PTS_1), INTENT(INOUT) :: PTS
    INTEGER, INTENT(IN) :: M
    INTEGER, INTENT(IN) :: Q_0
    INTEGER, INTENT(IN) :: Q_1
    REAL(KIND=R8), DIMENSION(Q_0,Q_1), INTENT(INOUT) :: Q
    INTEGER, INTENT(IN) :: SIMPS_0
    INTEGER, INTENT(IN) :: SIMPS_1
    INTEGER, DIMENSION(SIMPS_0,SIMPS_1), INTENT(OUT) :: SIMPS
    INTEGER, INTENT(IN) :: WEIGHTS_0
    INTEGER, INTENT(IN) :: WEIGHTS_1
    REAL(KIND=R8), DIMENSION(WEIGHTS_0,WEIGHTS_1), INTENT(OUT) :: &
         &WEIGHTS
    INTEGER, INTENT(IN) :: IERR_0
    INTEGER, DIMENSION(IERR_0), INTENT(OUT) :: IERR
    LOGICAL, INTENT(IN) :: INTERP_IN_PRESENT
    INTEGER, INTENT(IN) :: INTERP_IN_0
    INTEGER, INTENT(IN) :: INTERP_IN_1
    REAL(KIND=R8), DIMENSION(INTERP_IN_0,INTERP_IN_1), INTENT(IN),&
         & OPTIONAL :: INTERP_IN
    LOGICAL, INTENT(IN) :: INTERP_OUT_PRESENT
    INTEGER, INTENT(IN) :: INTERP_OUT_0
    INTEGER, INTENT(IN) :: INTERP_OUT_1
    REAL(KIND=R8), DIMENSION(INTERP_OUT_0,INTERP_OUT_1), INTENT(OU&
         &T), OPTIONAL :: INTERP_OUT
    LOGICAL, INTENT(IN) :: EPS_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EPS
    LOGICAL, INTENT(IN) :: EXTRAP_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EXTRAP
    LOGICAL, INTENT(IN) :: RNORM_PRESENT
    INTEGER, INTENT(IN) :: RNORM_0
    REAL(KIND=R8), DIMENSION(RNORM_0), INTENT(OUT), OPTIONAL :: RN&
         &ORM
    LOGICAL, INTENT(IN) :: IBUDGET_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: IBUDGET
    LOGICAL, INTENT(IN) :: CHAIN_PRESENT
    LOGICAL, INTENT(IN), OPTIONAL :: CHAIN
    ! Preparation code before function call (copying character arrays)
    
    ! Calling source fortran function
    IF (INTERP_IN_PRESENT) THEN
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM, IBUDGET=IBUDGET, CHAI&
                              &N=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, IBUDGET=IB&
                              &UDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, IBUDGET=IB&
                              &UDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, CHAIN=CHAI&
                              &N)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, IBUD&
                              &GET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, IBUD&
                              &GET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, CHAI&
                              &N=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM, IBUDGE&
                              &T=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM, IBUDGE&
                              &T=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM, CHAIN=&
                              &CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, IBUDGET=IBUDGET, CH&
                              &AIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                              &ET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                              &ET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM, CHAIN&
                              &=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET, C&
                              &HAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                              &IN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUDGE&
                              &T, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUDGE&
                              &T)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, IBUDGET=IBUDGET, CHAIN=CHAI&
                              &N)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, IBU&
                              &DGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, IBU&
                              &DGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, CHA&
                              &IN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ELSE
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                              &DGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                              &DGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM, CHA&
                              &IN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET,&
                              & CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM, IBUDGET=IBUDGET, C&
                              &HAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUD&
                              &GET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUD&
                              &GET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, IBUDGET=IBUDGET, CHAIN=CH&
                              &AIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM, IBUDGET=IBUDGET, CHAIN=CHAI&
                              &N)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, I&
                              &BUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, I&
                              &BUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, C&
                              &HAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM, IBUDGET=IBUDGET, CHAIN=CHA&
                              &IN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM, IB&
                              &UDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM, IB&
                              &UDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM, CH&
                              &AIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, IBUDGET=IBUDGET&
                              &, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, IBUDGET=IBUDGET&
                              &)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, IBUDGET=I&
                              &BUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, IBUDGET=I&
                              &BUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, CHAIN=CHA&
                              &IN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM, IBUDGET=IBU&
                              &DGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM, IBUDGET=IBU&
                              &DGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM, CHAIN=CHAIN&
                              &)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, IBUDGET=IBUDGET, CHAIN=C&
                              &HAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ENDIF
    ! Copying out any necessary values for return (character arrays)
    
  END SUBROUTINE c_DELAUNAYSPARSES


  !     Fortran wrapper for DELAUNAYSPARSEP, callable from C     
  !========================================================

   SUBROUTINE c_DELAUNAYSPARSEP( D, N, PTS_0, PTS_1, PTS, M, Q_0, Q_1,&
     & Q, SIMPS_0, SIMPS_1, SIMPS, WEIGHTS_0, WEIGHTS_1, WEIGHTS, IERR&
     &_0, IERR, INTERP_IN_0, INTERP_IN_1, INTERP_IN, INTERP_IN_PRESENT&
     &, INTERP_OUT_0, INTERP_OUT_1, INTERP_OUT, INTERP_OUT_PRESENT, EP&
     &S, EPS_PRESENT, EXTRAP, EXTRAP_PRESENT, RNORM_0, RNORM, RNORM_PR&
     &ESENT, IBUDGET, IBUDGET_PRESENT, CHAIN, CHAIN_PRESENT, PMODE, PM&
     &ODE_PRESENT, CHUNKSIZE, CHUNKSIZE_PRESENT ) BIND(c)
    ! This subroutine's only purpose is to call the source fortran
    !  code while also being accessible from C.
    INTEGER, INTENT(IN) :: D
    INTEGER, INTENT(IN) :: N
    INTEGER, INTENT(IN) :: PTS_0
    INTEGER, INTENT(IN) :: PTS_1
    REAL(KIND=R8), DIMENSION(PTS_0,PTS_1), INTENT(INOUT) :: PTS
    INTEGER, INTENT(IN) :: M
    INTEGER, INTENT(IN) :: Q_0
    INTEGER, INTENT(IN) :: Q_1
    REAL(KIND=R8), DIMENSION(Q_0,Q_1), INTENT(INOUT) :: Q
    INTEGER, INTENT(IN) :: SIMPS_0
    INTEGER, INTENT(IN) :: SIMPS_1
    INTEGER, DIMENSION(SIMPS_0,SIMPS_1), INTENT(OUT) :: SIMPS
    INTEGER, INTENT(IN) :: WEIGHTS_0
    INTEGER, INTENT(IN) :: WEIGHTS_1
    REAL(KIND=R8), DIMENSION(WEIGHTS_0,WEIGHTS_1), INTENT(OUT) :: &
         &WEIGHTS
    INTEGER, INTENT(IN) :: IERR_0
    INTEGER, DIMENSION(IERR_0), INTENT(OUT) :: IERR
    LOGICAL, INTENT(IN) :: INTERP_IN_PRESENT
    INTEGER, INTENT(IN) :: INTERP_IN_0
    INTEGER, INTENT(IN) :: INTERP_IN_1
    REAL(KIND=R8), DIMENSION(INTERP_IN_0,INTERP_IN_1), INTENT(IN),&
         & OPTIONAL :: INTERP_IN
    LOGICAL, INTENT(IN) :: INTERP_OUT_PRESENT
    INTEGER, INTENT(IN) :: INTERP_OUT_0
    INTEGER, INTENT(IN) :: INTERP_OUT_1
    REAL(KIND=R8), DIMENSION(INTERP_OUT_0,INTERP_OUT_1), INTENT(OU&
         &T), OPTIONAL :: INTERP_OUT
    LOGICAL, INTENT(IN) :: EPS_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EPS
    LOGICAL, INTENT(IN) :: EXTRAP_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EXTRAP
    LOGICAL, INTENT(IN) :: RNORM_PRESENT
    INTEGER, INTENT(IN) :: RNORM_0
    REAL(KIND=R8), DIMENSION(RNORM_0), INTENT(OUT), OPTIONAL :: RN&
         &ORM
    LOGICAL, INTENT(IN) :: IBUDGET_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: IBUDGET
    LOGICAL, INTENT(IN) :: CHAIN_PRESENT
    LOGICAL, INTENT(IN), OPTIONAL :: CHAIN
    LOGICAL, INTENT(IN) :: PMODE_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: PMODE
    LOGICAL, INTENT(IN) :: CHUNKSIZE_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: CHUNKSIZE
    ! Preparation code before function call (copying character arrays)
    
    ! Calling source fortran function
    IF (INTERP_IN_PRESENT) THEN
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                    &PMODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                    &PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, PMODE=PMODE, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, CHAIN=CHAIN, PMODE=PMODE, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, CHAIN=CHAIN, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, PMODE=PMODE, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, RNORM=RNOR&
                                    &M)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, CHAIN=CHAIN, PMODE=PMODE, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, CHAIN=CHAIN, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, PMODE=PMODE, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IB&
                                    &UDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, CHAIN=CHAI&
                                    &N, PMODE=PMODE, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, CHAIN=CHAI&
                                    &N, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, CHAIN=CHAI&
                                    &N, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, CHAIN=CHAI&
                                    &N)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, PMODE=PMOD&
                                    &E, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, PMODE=PMOD&
                                    &E)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, EXTRAP=EXTRAP)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN, PMODE=PMODE, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, PMODE=PMODE, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, IBUDGET=IBUD&
                                    &GET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, CHAIN=CHAIN,&
                                    & PMODE=PMODE, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, CHAIN=CHAIN,&
                                    & PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, CHAIN=CHAIN,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, PMODE=PMODE,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, CHAIN=CH&
                                    &AIN, PMODE=PMODE, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, CHAIN=CH&
                                    &AIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, CHAIN=CH&
                                    &AIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, CHAIN=CH&
                                    &AIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, PMODE=PM&
                                    &ODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, PMODE=PM&
                                    &ODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, CHAIN=CHAIN, PMODE=PMODE,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, CHAIN=CHAIN, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, PMODE=PMODE, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &PS=EPS)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, CHAIN=CHAIN, PMODE=PMO&
                                    &DE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, CHAIN=CHAIN, PMODE=PMO&
                                    &DE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, CHAIN=CHAIN, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, PMODE=PMODE, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, IBUDGE&
                                    &T=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=&
                                    &CHAIN, PMODE=PMODE, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=&
                                    &CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=&
                                    &CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=&
                                    &CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, PMODE=&
                                    &PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, PMODE=&
                                    &PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, CH&
                                    &AIN=CHAIN, PMODE=PMODE, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, CH&
                                    &AIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, CH&
                                    &AIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, CH&
                                    &AIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, PM&
                                    &ODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, PM&
                                    &ODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, CHAIN=CHAIN, PMODE=&
                                    &PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, CHAIN=CHAIN, PMODE=&
                                    &PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, CHAIN=CHAIN, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, PMODE=PMODE, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, E&
                                    &XTRAP=EXTRAP)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN, PMODE=PMODE, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, PMOD&
                                    &E=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, PMOD&
                                    &E=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, CHAIN=CHAIN, PMODE=PM&
                                    &ODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, CHAIN=CHAIN, PMODE=PM&
                                    &ODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, CHAIN=CHAIN, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, PMODE=PMODE, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, R&
                                    &NORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, CHAIN=CHAIN, PMOD&
                                    &E=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, CHAIN=CHAIN, PMOD&
                                    &E=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, CHAIN=CHAIN, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, PMODE=PMODE, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, I&
                                    &BUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, C&
                                    &HAIN=CHAIN, PMODE=PMODE, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, C&
                                    &HAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, C&
                                    &HAIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, C&
                                    &HAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, P&
                                    &MODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, P&
                                    &MODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT, C&
                                    &HUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, INTERP_OUT=INTERP_OUT)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                                    &IN=CHAIN, PMODE=PMODE, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                                    &IN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                                    &IN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                                    &IN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, PMO&
                                    &DE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, PMO&
                                    &DE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, CHAIN=CHAIN, PMODE=P&
                                    &MODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, CHAIN=CHAIN, PMODE=P&
                                    &MODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, CHAIN=CHAIN, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, PMODE=PMODE, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN, PMO&
                                    &DE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN, PMO&
                                    &DE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, PMODE=PMODE, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &CHAIN=CHAIN, PMODE=PMODE, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &CHAIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &PMODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, EXTRAP=EXTRAP)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, CHAIN=CHAIN, PMODE&
                                    &=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, CHAIN=CHAIN, PMODE&
                                    &=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, CHAIN=CHAIN, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, PMODE=PMODE, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, IB&
                                    &UDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, CH&
                                    &AIN=CHAIN, PMODE=PMODE, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, CH&
                                    &AIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, CH&
                                    &AIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, CH&
                                    &AIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, PM&
                                    &ODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, PM&
                                    &ODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, CHAIN=CHAIN, PMODE=PMODE, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, CHAIN=CHAIN, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, PMODE=PMODE, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, IBUDGET=IBUDGET&
                                    &)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, CHAIN=CHAIN, PM&
                                    &ODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, CHAIN=CHAIN, PM&
                                    &ODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, CHAIN=CHAIN, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, PMODE=PMODE, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EPS=EPS)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, CHAIN=CHAIN,&
                                    & PMODE=PMODE, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, CHAIN=CHAIN,&
                                    & PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, CHAIN=CHAIN,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, PMODE=PMODE,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, CHAIN=CHAIN, PMODE=PMODE, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, CHAIN=CHAIN, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, PMODE=PMODE, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, RNORM=RNO&
                                    &RM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN, PMODE=PMODE,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, PMODE=PMODE, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, IBUDGET=I&
                                    &BUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, CHAIN=CHA&
                                    &IN, PMODE=PMODE, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, CHAIN=CHA&
                                    &IN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, CHAIN=CHA&
                                    &IN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, CHAIN=CHA&
                                    &IN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, PMODE=PMO&
                                    &DE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, PMODE=PMO&
                                    &DE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, EXTRAP=EXTRAP)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, CHAIN=CHAIN, PMODE=PMODE, C&
                                    &HUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, CHAIN=CHAIN, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, PMODE=PMODE, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, IBUDGET=IBU&
                                    &DGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, CHAIN=CHAIN&
                                    &, PMODE=PMODE, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, CHAIN=CHAIN&
                                    &, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, CHAIN=CHAIN&
                                    &, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, CHAIN=CHAIN&
                                    &)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, PMODE=PMODE&
                                    &, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, PMODE=PMODE&
                                    &)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, CHAIN=C&
                                    &HAIN, PMODE=PMODE, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, CHAIN=C&
                                    &HAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, CHAIN=C&
                                    &HAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, CHAIN=C&
                                    &HAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, PMODE=P&
                                    &MODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, PMODE=P&
                                    &MODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, CHAIN=CHAIN, PMODE=PMODE&
                                    &, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, CHAIN=CHAIN, PMODE=PMODE&
                                    &)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, CHAIN=CHAIN, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, PMODE=PMODE, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_IN=IN&
                                    &TERP_IN)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ELSE
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                    &HAIN=CHAIN, PMODE=PMODE, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                    &HAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                    &HAIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                    &HAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, P&
                                    &MODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, P&
                                    &MODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                    &HUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, CHAIN=CHAIN, PMODE&
                                    &=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, CHAIN=CHAIN, PMODE&
                                    &=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, CHAIN=CHAIN, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, PMODE=PMODE, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, CHAIN=CHAIN, P&
                                    &MODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, CHAIN=CHAIN, P&
                                    &MODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, CHAIN=CHAIN, C&
                                    &HUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, PMODE=PMODE, C&
                                    &HUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, CHAIN=CHAIN, PMODE=PMODE, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, CHAIN=CHAIN, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, PMODE=PMODE, CHUNKSIZE=CHUNKSIZ&
                                    &E)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, EXTRAP=EXTRAP&
                                    &)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN, PMO&
                                    &DE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN, PMO&
                                    &DE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, PMODE=PMODE, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &CHAIN=CHAIN, PMODE=PMODE, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &CHAIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &PMODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, CHAIN=CHAIN, PMODE=PMODE, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, CHAIN=CHAIN, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, PMODE=PMODE, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, IBUDGET=IBUDG&
                                    &ET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, CHAIN=CHAIN, &
                                    &PMODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, CHAIN=CHAIN, &
                                    &PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, CHAIN=CHAIN, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, PMODE=PMODE, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EPS=EPS)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, CHAIN=CHAI&
                                    &N, PMODE=PMODE, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, CHAIN=CHAI&
                                    &N, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, CHAIN=CHAI&
                                    &N, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, CHAIN=CHAI&
                                    &N)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, PMODE=PMOD&
                                    &E, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, PMODE=PMOD&
                                    &E)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, CHAIN=CHAIN, PMODE=PMODE, C&
                                    &HUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, CHAIN=CHAIN, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, PMODE=PMODE, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, RNORM=R&
                                    &NORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, CHAIN=CHAIN, PMODE=PMOD&
                                    &E, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, CHAIN=CHAIN, PMODE=PMOD&
                                    &E)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, CHAIN=CHAIN, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, PMODE=PMODE, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, IBUDGET&
                                    &=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, CHAIN=C&
                                    &HAIN, PMODE=PMODE, CHUNKSIZE=CHUN&
                                    &KSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, CHAIN=C&
                                    &HAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, CHAIN=C&
                                    &HAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, CHAIN=C&
                                    &HAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, PMODE=P&
                                    &MODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, PMODE=P&
                                    &MODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, EXTRAP=EXTRAP)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN, PMODE=PMODE,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, PMODE=PMODE, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, IBUDGET=I&
                                    &BUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, CHAIN=CHA&
                                    &IN, PMODE=PMODE, CHUNKSIZE=CHUNKS&
                                    &IZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, CHAIN=CHA&
                                    &IN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, CHAIN=CHA&
                                    &IN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, CHAIN=CHA&
                                    &IN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, PMODE=PMO&
                                    &DE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, PMODE=PMO&
                                    &DE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, CHAIN&
                                    &=CHAIN, PMODE=PMODE, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, CHAIN&
                                    &=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, CHAIN&
                                    &=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, CHAIN&
                                    &=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, PMODE&
                                    &=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, PMODE&
                                    &=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET, CHUNK&
                                    &SIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, CHAIN=CHAIN, PMODE=PMO&
                                    &DE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, CHAIN=CHAIN, PMODE=PMO&
                                    &DE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, CHAIN=CHAIN, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, PMODE=PMODE, CHUNKSIZE&
                                    &=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, INTERP_OUT=I&
                                    &NTERP_OUT)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, CHAIN=CHAIN, PMODE=PMODE&
                                    &, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, CHAIN=CHAIN, PMODE=PMODE&
                                    &)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, CHAIN=CHAIN, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, PMODE=PMODE, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, IBUDGET=&
                                    &IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, CHAIN=CH&
                                    &AIN, PMODE=PMODE, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, CHAIN=CH&
                                    &AIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, CHAIN=CH&
                                    &AIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, CHAIN=CH&
                                    &AIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, PMODE=PM&
                                    &ODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, PMODE=PM&
                                    &ODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN, PMODE=PMODE, CHUNKSIZE=C&
                                    &HUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, CHAI&
                                    &N=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, PMOD&
                                    &E=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, PMOD&
                                    &E=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, CHAIN=CHAIN, PMODE=PM&
                                    &ODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, CHAIN=CHAIN, PMODE=PM&
                                    &ODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, CHAIN=CHAIN, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, PMODE=PMODE, CHUNKSIZ&
                                    &E=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, EXT&
                                    &RAP=EXTRAP)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, CHAIN=&
                                    &CHAIN, PMODE=PMODE, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, CHAIN=&
                                    &CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, CHAIN=&
                                    &CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, CHAIN=&
                                    &CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, PMODE=&
                                    &PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, PMODE=&
                                    &PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, CHAIN=CHAIN, PMODE=PMOD&
                                    &E, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, CHAIN=CHAIN, PMODE=PMOD&
                                    &E)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, CHAIN=CHAIN, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, PMODE=PMODE, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, RNO&
                                    &RM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, CHAIN=CHAIN, PMODE=&
                                    &PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, CHAIN=CHAIN, PMODE=&
                                    &PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, CHAIN=CHAIN, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, PMODE=PMODE, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, IBU&
                                    &DGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, CHA&
                                    &IN=CHAIN, PMODE=PMODE, CHUNKSIZE=&
                                    &CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, CHA&
                                    &IN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, CHA&
                                    &IN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, CHA&
                                    &IN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, PMO&
                                    &DE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, PMO&
                                    &DE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS, CHU&
                                    &NKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EPS=EPS)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &CHAIN=CHAIN, PMODE=PMODE, CHUNKSI&
                                    &ZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &CHAIN=CHAIN, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &PMODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, CHAIN=CHAIN, PMOD&
                                    &E=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, CHAIN=CHAIN, PMOD&
                                    &E=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, CHAIN=CHAIN, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, PMODE=PMODE, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                    &PMODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                    &PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, PMODE=PMODE, &
                                    &CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET, CHUNKSIZE=CHU&
                                    &NKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, CHAIN=CHAIN, PMODE=PMODE, CHUN&
                                    &KSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, CHAIN=CHAIN, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, PMODE=PMODE, CHUNKSIZE=CHUNKSI&
                                    &ZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, EXTRAP=EXTRA&
                                    &P)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, CHAIN=CHAIN, PM&
                                    &ODE=PMODE, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, CHAIN=CHAIN, PM&
                                    &ODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, CHAIN=CHAIN, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, PMODE=PMODE, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & IBUDGET=IBUDGET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & CHAIN=CHAIN, PMODE=PMODE, CHUNKS&
                                    &IZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & CHAIN=CHAIN, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & PMODE=PMODE, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, RNORM=RNORM)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN, PMODE=PMODE, CH&
                                    &UNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, PMODE=PMODE, CHUNKSIZE=CHUNK&
                                    &SIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET, CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, IBUDGET=IBUD&
                                    &GET)
                            ENDIF
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, CHAIN=CHAIN,&
                                    & PMODE=PMODE, CHUNKSIZE=CHUNKSIZE&
                                    &)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, CHAIN=CHAIN,&
                                    & PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, CHAIN=CHAIN,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, CHAIN=CHAIN)
                            ENDIF
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, PMODE=PMODE,&
                                    & CHUNKSIZE=CHUNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, PMODE=PMODE)
                            ENDIF
                         ELSE
                            IF (CHUNKSIZE_PRESENT) THEN
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR, CHUNKSIZE=CH&
                                    &UNKSIZE)
                            ELSE
                               CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, S&
                                    &IMPS, WEIGHTS, IERR)
                            ENDIF
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ENDIF
    ! Copying out any necessary values for return (character arrays)
    
  END SUBROUTINE c_DELAUNAYSPARSEP

END MODULE DELAUNAY_WRAPPER
