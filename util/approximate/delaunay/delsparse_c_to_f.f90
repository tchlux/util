!     Module generated by fmodpy for fortran file named 'DELSPARSE'     
!=====================================================================
MODULE DELSPARSE_WRAPPER
  ! Any necessary "USE" statements
  USE REAL_PRECISION
  USE DELSPARSE_MOD
  ! Make sure it is clear that no implicit types are used in this wrapper
  IMPLICIT NONE
  ! Any interfaces defined for "PROCEDURE" declarations
  
CONTAINS
  !     Fortran wrapper for DELAUNAYSPARSES, callable from C     
  !========================================================

   SUBROUTINE c_DELAUNAYSPARSES( D, N, PTS_0, PTS_1, PTS, M, Q_0, Q_1,&
     & Q, SIMPS_0, SIMPS_1, SIMPS, WEIGHTS_0, WEIGHTS_1, WEIGHTS, IERR&
     &_0, IERR, INTERP_IN_0, INTERP_IN_1, INTERP_IN, INTERP_IN_PRESENT&
     &, INTERP_OUT_0, INTERP_OUT_1, INTERP_OUT, INTERP_OUT_PRESENT, EP&
     &S, EPS_PRESENT, EXTRAP, EXTRAP_PRESENT, RNORM_0, RNORM, RNORM_PR&
     &ESENT, IBUDGET, IBUDGET_PRESENT, CHAIN, CHAIN_PRESENT ) BIND(c)
    ! This subroutine's only purpose is to call the source fortran
    !  code while also being accessible from C.
    INTEGER, INTENT(IN) :: D
    INTEGER, INTENT(IN) :: N
    INTEGER, INTENT(IN) :: PTS_0
    INTEGER, INTENT(IN) :: PTS_1
    REAL(KIND=R8), DIMENSION(PTS_0,PTS_1), INTENT(INOUT) :: PTS
    INTEGER, INTENT(IN) :: M
    INTEGER, INTENT(IN) :: Q_0
    INTEGER, INTENT(IN) :: Q_1
    REAL(KIND=R8), DIMENSION(Q_0,Q_1), INTENT(INOUT) :: Q
    INTEGER, INTENT(IN) :: SIMPS_0
    INTEGER, INTENT(IN) :: SIMPS_1
    INTEGER, DIMENSION(SIMPS_0,SIMPS_1), INTENT(OUT) :: SIMPS
    INTEGER, INTENT(IN) :: WEIGHTS_0
    INTEGER, INTENT(IN) :: WEIGHTS_1
    REAL(KIND=R8), DIMENSION(WEIGHTS_0,WEIGHTS_1), INTENT(OUT) :: WEIG&
         &HTS
    INTEGER, INTENT(IN) :: IERR_0
    INTEGER, DIMENSION(IERR_0), INTENT(OUT) :: IERR
    LOGICAL, INTENT(IN) :: INTERP_IN_PRESENT
    INTEGER, INTENT(IN) :: INTERP_IN_0
    INTEGER, INTENT(IN) :: INTERP_IN_1
    REAL(KIND=R8), DIMENSION(INTERP_IN_0,INTERP_IN_1), INTENT(IN), OPT&
         &IONAL :: INTERP_IN
    LOGICAL, INTENT(IN) :: INTERP_OUT_PRESENT
    INTEGER, INTENT(IN) :: INTERP_OUT_0
    INTEGER, INTENT(IN) :: INTERP_OUT_1
    REAL(KIND=R8), DIMENSION(INTERP_OUT_0,INTERP_OUT_1), INTENT(OUT), &
         &OPTIONAL :: INTERP_OUT
    LOGICAL, INTENT(IN) :: EPS_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EPS
    LOGICAL, INTENT(IN) :: EXTRAP_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EXTRAP
    LOGICAL, INTENT(IN) :: RNORM_PRESENT
    INTEGER, INTENT(IN) :: RNORM_0
    REAL(KIND=R8), DIMENSION(RNORM_0), INTENT(OUT), OPTIONAL :: RNORM
    LOGICAL, INTENT(IN) :: IBUDGET_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: IBUDGET
    LOGICAL, INTENT(IN) :: CHAIN_PRESENT
    LOGICAL, INTENT(IN), OPTIONAL :: CHAIN
    ! Preparation code before function call (copying character arrays)
    
    ! Calling source fortran function
    IF (INTERP_IN_PRESENT) THEN
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM, IBUDGET=IBUDGET, CHAI&
                              &N=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, EXTRAP=EXT&
                              &RAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, RNORM=RNOR&
                              &M)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, IBUDGET=IB&
                              &UDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, IBUDGET=IB&
                              &UDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS, CHAIN=CHAI&
                              &N)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EPS=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, RNOR&
                              &M=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, IBUD&
                              &GET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, IBUD&
                              &GET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP, CHAI&
                              &N=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM, IBUDGE&
                              &T=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM, IBUDGE&
                              &T=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM, CHAIN=&
                              &CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, IBUDGET=IBUDGET, CH&
                              &AIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, INT&
                              &ERP_OUT=INTERP_OUT)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                              &ET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                              &ET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM, CHAIN&
                              &=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET, C&
                              &HAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                              &IN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EPS&
                              &=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUDGE&
                              &T, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUDGE&
                              &T)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, IBUDGET=IBUDGET, CHAIN=CHAI&
                              &N)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, EXT&
                              &RAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, RNO&
                              &RM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, IBU&
                              &DGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, IBU&
                              &DGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN, CHA&
                              &IN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_IN=INTERP_IN)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ELSE
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                              &DGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                              &DGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM, CHA&
                              &IN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET,&
                              & CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM, IBUDGET=IBUDGET, C&
                              &HAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &PS=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUD&
                              &GET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=IBUD&
                              &GET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, IBUDGET=IBUDGET, CHAIN=CH&
                              &AIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, E&
                              &XTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM, IBUDGET=IBUDGET, CHAIN=CHAI&
                              &N)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, R&
                              &NORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, I&
                              &BUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, I&
                              &BUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT, C&
                              &HAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, INTERP_OUT=INTERP_OUT)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM, IBUDGET=IBUDGET, CHAIN=CHA&
                              &IN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP, &
                              &CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM, IB&
                              &UDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM, IB&
                              &UDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM, CH&
                              &AIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, IBUDGET=IBUDGET&
                              &, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, IBUDGET=IBUDGET&
                              &)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EPS=EPS)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, RNORM=RNO&
                              &RM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, IBUDGET=I&
                              &BUDGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, IBUDGET=I&
                              &BUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP, CHAIN=CHA&
                              &IN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, EXTRAP=EXTRAP)
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM, IBUDGET=IBU&
                              &DGET, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM, IBUDGET=IBU&
                              &DGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM, CHAIN=CHAIN&
                              &)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, RNORM=RNORM)
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, IBUDGET=IBUDGET, CHAIN=C&
                              &HAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, IBUDGET=IBUDGET)
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR, CHAIN=CHAIN)
                      ELSE
                         CALL DELAUNAYSPARSES(D, N, PTS, M, Q, SIMPS, &
                              &WEIGHTS, IERR)
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ENDIF
    ! Copying out any necessary values for return (character arrays)
    
  END SUBROUTINE c_DELAUNAYSPARSES


  !     Fortran wrapper for DELAUNAYSPARSEP, callable from C     
  !========================================================

   SUBROUTINE c_DELAUNAYSPARSEP( D, N, PTS_0, PTS_1, PTS, M, Q_0, Q_1,&
     & Q, SIMPS_0, SIMPS_1, SIMPS, WEIGHTS_0, WEIGHTS_1, WEIGHTS, IERR&
     &_0, IERR, INTERP_IN_0, INTERP_IN_1, INTERP_IN, INTERP_IN_PRESENT&
     &, INTERP_OUT_0, INTERP_OUT_1, INTERP_OUT, INTERP_OUT_PRESENT, EP&
     &S, EPS_PRESENT, EXTRAP, EXTRAP_PRESENT, RNORM_0, RNORM, RNORM_PR&
     &ESENT, IBUDGET, IBUDGET_PRESENT, CHAIN, CHAIN_PRESENT, PMODE, PM&
     &ODE_PRESENT ) BIND(c)
    ! This subroutine's only purpose is to call the source fortran
    !  code while also being accessible from C.
    INTEGER, INTENT(IN) :: D
    INTEGER, INTENT(IN) :: N
    INTEGER, INTENT(IN) :: PTS_0
    INTEGER, INTENT(IN) :: PTS_1
    REAL(KIND=R8), DIMENSION(PTS_0,PTS_1), INTENT(INOUT) :: PTS
    INTEGER, INTENT(IN) :: M
    INTEGER, INTENT(IN) :: Q_0
    INTEGER, INTENT(IN) :: Q_1
    REAL(KIND=R8), DIMENSION(Q_0,Q_1), INTENT(INOUT) :: Q
    INTEGER, INTENT(IN) :: SIMPS_0
    INTEGER, INTENT(IN) :: SIMPS_1
    INTEGER, DIMENSION(SIMPS_0,SIMPS_1), INTENT(OUT) :: SIMPS
    INTEGER, INTENT(IN) :: WEIGHTS_0
    INTEGER, INTENT(IN) :: WEIGHTS_1
    REAL(KIND=R8), DIMENSION(WEIGHTS_0,WEIGHTS_1), INTENT(OUT) :: WEIG&
         &HTS
    INTEGER, INTENT(IN) :: IERR_0
    INTEGER, DIMENSION(IERR_0), INTENT(OUT) :: IERR
    LOGICAL, INTENT(IN) :: INTERP_IN_PRESENT
    INTEGER, INTENT(IN) :: INTERP_IN_0
    INTEGER, INTENT(IN) :: INTERP_IN_1
    REAL(KIND=R8), DIMENSION(INTERP_IN_0,INTERP_IN_1), INTENT(IN), OPT&
         &IONAL :: INTERP_IN
    LOGICAL, INTENT(IN) :: INTERP_OUT_PRESENT
    INTEGER, INTENT(IN) :: INTERP_OUT_0
    INTEGER, INTENT(IN) :: INTERP_OUT_1
    REAL(KIND=R8), DIMENSION(INTERP_OUT_0,INTERP_OUT_1), INTENT(OUT), &
         &OPTIONAL :: INTERP_OUT
    LOGICAL, INTENT(IN) :: EPS_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EPS
    LOGICAL, INTENT(IN) :: EXTRAP_PRESENT
    REAL(KIND=R8), INTENT(IN), OPTIONAL :: EXTRAP
    LOGICAL, INTENT(IN) :: RNORM_PRESENT
    INTEGER, INTENT(IN) :: RNORM_0
    REAL(KIND=R8), DIMENSION(RNORM_0), INTENT(OUT), OPTIONAL :: RNORM
    LOGICAL, INTENT(IN) :: IBUDGET_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: IBUDGET
    LOGICAL, INTENT(IN) :: CHAIN_PRESENT
    LOGICAL, INTENT(IN), OPTIONAL :: CHAIN
    LOGICAL, INTENT(IN) :: PMODE_PRESENT
    INTEGER, INTENT(IN), OPTIONAL :: PMODE
    ! Preparation code before function call (copying character arrays)
    
    ! Calling source fortran function
    IF (INTERP_IN_PRESENT) THEN
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=CHA&
                                 &IN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, CHAIN=CHA&
                                 &IN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, IBUDGET=IBUDGET, CHAIN&
                                 &=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, IBUDGET=IBUDGET, CHAIN&
                                 &=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, IBUDGET=IBUDGET, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, CHAIN=CHAIN, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, E&
                                 &XTRAP=EXTRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, IBUDGET=IBUDGET, CHAIN=C&
                                 &HAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, IBUDGET=IBUDGET, CHAIN=C&
                                 &HAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, IBUDGET=IBUDGET, PMODE=P&
                                 &MODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, CHAIN=CHAIN, PMODE=PMODE&
                                 &)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, R&
                                 &NORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, I&
                                 &BUDGET=IBUDGET, CHAIN=CHAIN, PMODE=P&
                                 &MODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, I&
                                 &BUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, I&
                                 &BUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, I&
                                 &BUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, C&
                                 &HAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, C&
                                 &HAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EPS=EPS)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, IBUDGET=IBUDGET, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, CHAIN=CHAIN, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, IBUDGET=IBUDGET, CHAIN=CHAIN, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, EXTRAP=EXT&
                                 &RAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, IBUDGET=IBUDGET, CHAIN=CHAIN, PMO&
                                 &DE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, RNORM=RNOR&
                                 &M)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, IBUDGET=IB&
                                 &UDGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, IBUDGET=IB&
                                 &UDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, IBUDGET=IB&
                                 &UDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, IBUDGET=IB&
                                 &UDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, CHAIN=CHAI&
                                 &N, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, CHAIN=CHAI&
                                 &N)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT, PMODE=PMOD&
                                 &E)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, INTERP_OUT=INTERP_OUT)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET, CHAIN=CHAIN, PM&
                                 &ODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, RNORM=RNO&
                                 &RM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, IBUDGET=I&
                                 &BUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, IBUDGET=I&
                                 &BUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, CHAIN=CHA&
                                 &IN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, CHAIN=CHA&
                                 &IN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, EXTRAP=EXTRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, IBUDGET=IBU&
                                 &DGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, IBUDGET=IBU&
                                 &DGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, IBUDGET=IBU&
                                 &DGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, IBUDGET=IBU&
                                 &DGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, CHAIN=CHAIN&
                                 &, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, CHAIN=CHAIN&
                                 &)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM, PMODE=PMODE&
                                 &)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, IBUDGET=IBUDGET, CHAIN=C&
                                 &HAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, IBUDGET=IBUDGET, CHAIN=C&
                                 &HAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, IBUDGET=IBUDGET, PMODE=P&
                                 &MODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, CHAIN=CHAIN, PMODE=PMODE&
                                 &)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EPS=EPS)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET, CHAIN=CHAIN, PMODE=PMODE&
                                 &)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, CHAIN&
                                 &=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, CHAIN&
                                 &=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, IBUDGET=IBUDGET, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, CHAIN=CHAIN, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, EXTRAP=EXTRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                                 &IN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, IBUDGET=IBUDGET, CHA&
                                 &IN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, IBUDGET=IBUDGET, PMO&
                                 &DE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, CHAIN=CHAIN, PMODE=P&
                                 &MODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, IBUDGET=IBUDGET, CHAIN=CHAIN, PMO&
                                 &DE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_IN=INTERP_I&
                                 &N)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ELSE
       IF (INTERP_OUT_PRESENT) THEN
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                 &PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, RNORM=R&
                                 &NORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, IBUDGET&
                                 &=IBUDGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, IBUDGET&
                                 &=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, IBUDGET&
                                 &=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, IBUDGET&
                                 &=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, CHAIN=C&
                                 &HAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, CHAIN=C&
                                 &HAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP, PMODE=P&
                                 &MODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, EXTRAP=EXTRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, IBUDGET=I&
                                 &BUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, CHAIN=CHA&
                                 &IN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, CHAIN=CHA&
                                 &IN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, IBUDGET=IBUDGET, CHAIN&
                                 &=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, IBUDGET=IBUDGET, CHAIN&
                                 &=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, IBUDGET=IBUDGET, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, CHAIN=CHAIN, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EPS=EPS)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                                 &DGET=IBUDGET, CHAIN=CHAIN, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                                 &DGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                                 &DGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, IBU&
                                 &DGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, CHA&
                                 &IN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, CHA&
                                 &IN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM, PMO&
                                 &DE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, IBUDGET=IBUDGET,&
                                 & CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, IBUDGET=IBUDGET,&
                                 & CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, IBUDGET=IBUDGET,&
                                 & PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, CHAIN=CHAIN, PMO&
                                 &DE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, EXTRAP=EXTRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, IBUDGET=IBUDGET, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, CHAIN=CHAIN, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, IBUDGET=IBUDGET, CHAIN=CHAIN, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, INTERP_OUT=INTERP_&
                                 &OUT)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ELSE
          IF (EPS_PRESENT) THEN
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, IBUDGET=IBUDGET, &
                                 &CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, IBUDGET=IBUDGET, &
                                 &CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, IBUDGET=IBUDGET, &
                                 &PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, CHAIN=CHAIN, PMOD&
                                 &E=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, IBUDGET=IBUDGET, CHAIN=CHAIN, &
                                 &PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, EXTRAP=EX&
                                 &TRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET, CHAIN=CHAIN, PM&
                                 &ODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, RNORM=RNO&
                                 &RM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, IBUDGET=I&
                                 &BUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, IBUDGET=I&
                                 &BUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, IBUDGET=I&
                                 &BUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, CHAIN=CHA&
                                 &IN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, CHAIN=CHA&
                                 &IN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EPS=EPS)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ELSE
             IF (EXTRAP_PRESENT) THEN
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, IBUDGET=IBUDGET, CHAIN=CHA&
                                 &IN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, IBUDGET=IBUDGET, CHAIN=CHA&
                                 &IN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, IBUDGET=IBUDGET, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, CHAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, RNO&
                                 &RM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, IBU&
                                 &DGET=IBUDGET, CHAIN=CHAIN, PMODE=PMO&
                                 &DE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, IBU&
                                 &DGET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, IBU&
                                 &DGET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, IBU&
                                 &DGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, CHA&
                                 &IN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, CHA&
                                 &IN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP, PMO&
                                 &DE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, EXTRAP=EXTRAP)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ELSE
                IF (RNORM_PRESENT) THEN
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET, CHAIN=CHAIN, PMODE=PMODE&
                                 &)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, IBUDG&
                                 &ET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, CHAIN&
                                 &=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, CHAIN&
                                 &=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, RNORM=RNORM)
                         ENDIF
                      ENDIF
                   ENDIF
                ELSE
                   IF (IBUDGET_PRESENT) THEN
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, IBUDGET=IBUDGET, C&
                                 &HAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, IBUDGET=IBUDGET, P&
                                 &MODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, IBUDGET=IBUDGET)
                         ENDIF
                      ENDIF
                   ELSE
                      IF (CHAIN_PRESENT) THEN
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, CHAIN=CHAIN, PMODE&
                                 &=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, CHAIN=CHAIN)
                         ENDIF
                      ELSE
                         IF (PMODE_PRESENT) THEN
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR, PMODE=PMODE)
                         ELSE
                            CALL DELAUNAYSPARSEP(D, N, PTS, M, Q, SIMP&
                                 &S, WEIGHTS, IERR)
                         ENDIF
                      ENDIF
                   ENDIF
                ENDIF
             ENDIF
          ENDIF
       ENDIF
    ENDIF
    ! Copying out any necessary values for return (character arrays)
    
  END SUBROUTINE c_DELAUNAYSPARSEP

END MODULE DELSPARSE_WRAPPER
